import { ReactElement } from "react"

export type ButtonSideIcons = {
  subdominantIcon?: ReactElement
  dominantIcon?: ReactElement
  dominantIconSide?: "left" | "right"
  loadingIcon?: ReactElement
}
